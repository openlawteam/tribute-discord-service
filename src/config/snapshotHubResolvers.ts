/**
 * Register unique Snapshot Hub resolver names
 */
export const SNAPSHOT_HUB_RESOLVERS = [
  /**
   * `erc-712` branch in `tributelabs/snapshot-hub` fork
   */
  'LEGACY_TRIBUTE',
  'LEGACY_TRIBUTE_DRAFT',
] as const;
